name: log_chain
description: A Dart package for structured diagnostic log chains, allowing async and sync flows to be captured as branch snapshots, rendered as text, or exported as JSON.
version: 1.0.0

repository: https://github.com/konstantinvoronov/log_chain
issue_tracker: https://github.com/konstantinvoronov/log_chain/issues

environment:
  sdk: ^3.8.1

topics:
  - logging
  - diagnostics
  - debugging
  - dart
  - json

dependencies:

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0